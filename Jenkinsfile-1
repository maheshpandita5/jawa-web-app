pipeline {
  agent { label 'linux' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') {
      steps {
        sh 'docker build -t maheshpandita5/java-web-app:latest .'
      }
    }
    stage('Scan') {
      steps {
        sh 'trivy maheshpandita5/java-web-app:latest'
      }
    }
  }
}
